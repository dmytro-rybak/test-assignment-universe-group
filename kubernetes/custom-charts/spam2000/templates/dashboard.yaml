apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ .Release.Name }}-dashboard
  labels:
    app.kubernetes.io/name: {{ .Chart.Name }}
    grafana_dashboard: "1"
  annotations:
    grafana_folder: "Applications"
data:
  spam2000.json: |
{{ .Files.Get "dashboards/spam2000.json" | indent 4 }}
